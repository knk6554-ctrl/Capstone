# 기존 `iot_example` 검토 결과

검토 대상은 사용자가 제공한 `iot_example` 폴더입니다. 원본은 수정하지 않았고 실제 `.env`의 키도 열람하지 않았습니다.

## 결론

기존 코드는 “카카오 장소 한 곳을 검색하고 도보 경로 원본 JSON을 저장하는 예제”로서는 대체로 맞습니다. 그러나 WAYBAND의 전체 소프트웨어라고 보기에는 지도 UI, 현재 위치 추적, 회전 시점 판정, 진동 명령, ToF 연동이 빠져 있습니다.

또한 기존 README의 다음 설명은 최신 공식 문서 기준으로 수정해야 합니다.

> `/v2/routing/walk` 응답은 현재 공식 공개 문서에서 안정성이 보장된 일반 API로 확인되지 않았습니다.

2026-08-03 확인 결과 카카오 공식 문서가 `GET https://dapi.kakao.com/v2/routing/walk`를 REST API 키로 호출하는 도보 경로 API로 공개하고 있습니다. `BROAD_FIRST`, `SHORTEST`, `ACCESSIBLE` 탐색 옵션과 현재 코드에서 사용한 응답 필드도 공식 문서에 나옵니다.

## 잘된 부분

- REST API 키를 `.env`로 분리하고 `.gitignore`에 포함했습니다.
- 인증 헤더 `Authorization: KakaoAK ...`와 `dapi.kakao.com` 호스트 사용이 맞습니다.
- 장소 검색 `/v2/local/search/keyword.json`과 도보 경로 `/v2/routing/walk` 요청 파라미터가 공식 규격에 맞습니다.
- `status == "OK"`와 `route` 존재 여부를 확인합니다.
- `legs[].steps[]`, `properties.guidance`, `path.points`를 공식 응답 구조에 맞게 읽습니다.
- 외부 API를 호출하지 않는 단위 테스트가 있습니다.
- 동명의 병원을 주소로 확인하는 방어 로직이 들어 있습니다.

## 보완이 필요한 부분

### 1. 프로젝트 핵심 기능 미구현

기존 코드는 한 번 경로를 조회해 파일로 저장한 뒤 끝납니다. 사용자 GPS와 회전 지점 사이 거리를 반복 계산하지 않으므로 “어느 위치에서 어느 팔찌를 울릴지” 결정할 수 없습니다. 지도에 선을 그리는 JavaScript UI와 하드웨어로 전달할 진동 프로토콜도 없습니다.

### 2. 장소 선택 방식의 일반화 부족

기본 출발지만 도로명주소를 특별히 검사하고, 나머지는 첫 번째 검색 결과를 사용합니다. 같은 이름의 장소가 여러 지역에 있으면 잘못 선택될 수 있습니다. 새 버전은 검색 후보와 주소를 화면에 보여주고 사용자가 직접 선택하게 했습니다.

### 3. 응답 검증이 얕음

`summarize_route()`는 `route["properties"]`와 필수 필드가 있다고 바로 가정합니다. 응답 변경이나 부분 데이터에서 `KeyError`, 잘못된 좌표에서 `TypeError`가 날 수 있습니다. 새 버전은 외부 응답을 도메인 모델로 변환하는 단계에서 좌표 범위와 필드 형식을 검사합니다.

### 4. 안내 문구와 진동 방향 연결 부재

카카오는 단계별 `guidance`를 문자열로 제공합니다. 기존 코드는 문자열을 모으기만 합니다. 새 버전은 좌·우·유턴·도착을 분류하고, 표현이 없는 단계는 전후 경로의 방위각 차이로 보조 추정합니다. 이 분류는 현장 응답 샘플로 계속 테스트해야 합니다.

### 5. 안전 상태 처리 부재

GPS 이탈, 위치 정확도, 중복 진동 억제, ToF 히스테리시스가 없습니다. 새 버전은 회전 준비/즉시 명령을 한 번씩만 만들고, 3회 연속 경로 이탈 시 별도 패턴을 냅니다. ToF는 `CRITICAL < WARNING < CLEAR` 기준을 분리해 경계값 주변의 떨림을 줄입니다.

### 6. 운영 관점 보완 필요

HTTP 재시도, 쿼터 제어, 사용자 인증, 영구 저장, 장치 인증, HTTPS, 모니터링이 없습니다. 새 버전도 시제품 범위라 이 중 영구 저장과 인증은 구현하지 않았으며 README에 후속 작업으로 명시했습니다.

## 새 버전에서 달라진 책임 분리

| 책임 | 기존 | 새 버전 |
|---|---|---|
| 장소 | 첫 결과 또는 주소 특례 | 후보 목록에서 사용자 선택 |
| 경로 | 원본 JSON 저장 | 검증된 경로 모델 + 지도용 좌표 |
| 방향 | 안내 문구 목록 | 문구/기하 기반 회전 분류 |
| 위치 | 없음 | GPS 반경·경로 이탈 판정 |
| 진동 | 없음 | 장치 독립 JSON 명령 + 순번 버퍼 |
| ToF | 없음 | 4방향 상태·히스테리시스·진동 명령 |
| 화면 | 콘솔 출력 | 접근성을 고려한 웹 지도/상태 화면 |

## 참고한 공식 문서

- [카카오맵 REST API](https://developers.kakao.com/docs/ko/kakaomap/rest-api)
- [카카오 지도 Web API 가이드](https://apis.map.kakao.com/web/guide/)
- [카카오 API 쿼터](https://developers.kakao.com/docs/ko/getting-started/quota)
