# WAYBAND 기능 정리

이 문서는 현재 코드(`wayband/`, `web/`)에 실제로 구현되어 있는 기능을 화면/기능 단위로 정리한 것입니다. 설치·실행 방법은 [README.md](README.md)를, 장치(벨트·팔찌) 통신 규격은 [docs/HARDWARE_PROTOCOL.md](docs/HARDWARE_PROTOCOL.md)를 참고하세요.

---

## 1. 경로 기록 모드 (`web/index.html` 맨 위 섹션)

보호자가 시각장애인과 함께 실제 경로를 걸으면서, 좌회전·우회전·횡단보도·계단·도착 지점을 그 자리에서 태깅해 경로 파일을 만드는 기능입니다.

**사용 흐름**

1. 경로 이름 입력 → "기록 시작" 클릭
2. 브라우저가 현재 GPS 위치를 확인하고 서버에 기록을 시작 (이 위치가 자동으로 `start` 지점이 됨)
3. 걸으면서 좌회전 / 우회전 / 횡단보도 / 계단 버튼을 그때그때 클릭 → 클릭한 시점의 GPS 좌표가 그 유형으로 저장되고, 지도에 마커+라벨이 바로 표시됨
4. 도착하면 "도착 지점 저장" 클릭
5. "경로 저장" 클릭 → `recorded_routes/route_N.json` 파일 생성

**저장되는 JSON 형태**

```json
{
  "route_id": 1,
  "name": "정문에서 공학관",
  "waypoints": [
    { "type": "start", "lat": 37.12345, "lon": 127.12345 },
    { "type": "left_turn", "lat": 37.1238, "lon": 127.1237 },
    { "type": "crosswalk", "lat": 37.124, "lon": 127.1239 },
    { "type": "destination", "lat": 37.1242, "lon": 127.1241 }
  ]
}
```

**담당 코드**: `wayband/recording.py`(도메인 모델·저장소), `wayband/api.py`의 `/api/recordings*` 엔드포인트, `web/app.js`의 "경로 기록 모드" 섹션.

**현재 한계**

- GPS는 벨트가 아니라 브라우저(휴대폰) 위치를 사용합니다. 벨트-웹 통신이 정해지면 위치 출처 교체가 필요합니다.
- 여러 사람이 동시에 기록해도 구분하는 로직은 없습니다(오늘 범위 밖).

---

## 2. 경로 안내 모드 (기록한 경로) — `web/index.html`의 "03-B" 섹션

경로 기록 모드로 저장한 `route_N.json`을 불러와, 실제로 다음 지점까지 거리를 계산하고 진동 명령을 만드는 기능입니다. PPT 슬라이드 9의 "경로 안내 모드"에 해당합니다.

**사용 흐름**

1. "저장된 경로" 드롭다운에서 경로 선택 (`GET /api/recordings` 목록)
2. "안내 시작" 클릭 → 서버가 `route_N.json`을 불러와 안내 세션을 시작하고, 지도에 저장된 지점들이 파란 선+마커로 표시됨
3. 브라우저 GPS가 갱신될 때마다 서버로 위치 전달 → 다음 지점까지 남은 거리와 안내 문구를 화면에 표시
4. 지점에 충분히 가까워지면 진동 명령이 만들어지고, "진동 명령 로그" 패널과 `/api/haptics` 폴링 버퍼에 동시에 쌓임

**지점 유형별 진동**

| 지점 유형 | 방식 |
|---|---|
| 좌회전 | 25m 안에서 준비(왼쪽 팔찌 1회) → 8m 안에서 지금 실행(왼쪽 팔찌 3회) |
| 우회전 | 좌회전과 동일하되 오른쪽 팔찌 |
| 횡단보도 | 8m 안에서 양쪽 팔찌 2회 (준비 단계 없음) |
| 계단 | 8m 안에서 양쪽 팔찌 3회, 더 촘촘한 간격 (준비 단계 없음) |
| 도착 | 8m 안에서 양쪽 팔찌 길게 2회 |

경로 이탈 판정(3회 연속 이탈 시 경고)도 카카오 자동 경로 모드와 같은 방식으로 동작합니다. 태깅된 지점들을 순서대로 이은 선을 경로로 간주해서 이탈 여부를 계산합니다.

**담당 코드**: `wayband/recorded_navigation.py`(`RecordedRouteNavigationSession`, `RecordedNavigationService`), API의 `POST /api/recorded-routes/{route_id}/start`·`POST /api/recorded-routes/{route_id}/location`, `web/app.js`의 "경로 안내 모드(기록한 경로)" 섹션.

**현재 한계**

- 여전히 브라우저 GPS를 씁니다. 벨트 GPS 연동 전까지는 임시입니다.
- 한 번에 하나의 안내 세션만 유지합니다(같은 route_id로 여러 명이 동시에 안내받는 상황은 고려 안 됨).
- "준비" 단계는 좌/우회전에만 있고, 횡단보도·계단·도착은 근접 시 한 번만 울립니다(PPT 진동 표 기준으로 맞춘 설계이며, 필요하면 조정 가능).

---

## 3. 카카오 자동 경로 모드 (기존 기능)

출발지·도착지를 검색해서 카카오 공식 도보 경로 API로 경로를 자동 생성하는 기능입니다.

**사용 흐름**

1. 출발지/도착지 검색어 입력 → 검색 결과 중 정확한 장소 선택 (동명이인 장소 문제를 막기 위해 후보를 직접 고르게 되어 있음)
2. 경로 방식 선택: 편안한 길 우선(ACCESSIBLE) / 넓은 길 우선(BROAD_FIRST) / 최단 경로(SHORTEST)
3. "경로 만들기" 클릭 → 카카오 `/v2/routing/walk` 호출 → 거리, 예상 시간, 단계별 안내 문구를 받아옴

**안내 문구 → 방향 분류**

카카오는 각 구간의 안내를 `guidance` 문자열로만 줍니다. 이 문자열을 좌회전/우회전/유턴/횡단보도/직진/도착으로 분류하고, 방향을 알 수 있는 단어가 없으면 이전·다음 구간의 방위각 차이로 좌/우/유턴을 추정합니다.

**담당 코드**: `wayband/kakao.py`(카카오 API 호출), `wayband/route_parser.py`(응답 검증·안내 분류).

---

## 4. 지도 표시

카카오 지도 JavaScript SDK로 경로를 화면에 그립니다.

- 생성된 경로를 `Polyline`으로 표시하고, 출발지·도착지에 마커를 찍습니다.
- 지도 범위(bounds)를 경로 전체가 보이도록 자동 조정합니다.
- 경로 기록 모드에서 태깅한 지점도 같은 지도 위에 마커+라벨로 표시되고, 걸어온 궤적은 노란 점선으로 그려집니다.

**담당 코드**: `web/app.js`의 `drawRoute`, `placeRecordingMarker`, `updateRecordingLine`.

---

## 5. GPS 실시간 안내 모드 (카카오 경로 기반)

카카오로 만든 경로를 따라 걸을 때, 브라우저 GPS로 현재 위치를 추적해서 회전 시점마다 진동 명령을 만드는 기능입니다.

**판정 로직**

- 회전 지점 반경 25m 안에 들어오면 "준비" 진동(1회), 8m 안에 들어오면 "지금 실행" 진동(3회) — 각각 한 번씩만 발생(중복 방지)
- GPS가 튀어서 회전 지점을 건너뛴 경우, 이후 모든 안내가 막히지 않도록 지나간 지점은 자동으로 건너뜀
- 경로에서 벗어난 상태가 3회 연속 감지되면 양쪽 팔찌에 이탈 경고 진동
- 위치 정확도(accuracy)를 이탈 판정 여유 거리에 반영 (최대 30m까지)

**담당 코드**: `wayband/navigation.py`(`NavigationSession`), `wayband/service.py`, API의 `POST /api/navigation/{route_id}/location`.

**현재 한계**: 좌/우/유턴/도착만 진동으로 연결되어 있고, 카카오 경로 안의 횡단보도·계단 구간은 진동을 만들지 않습니다(분류는 되지만 haptic 이벤트로 연결 안 됨).

---

## 6. 진동 명령 시스템

실제 팔찌(ESP32) 장치와 서버 사이를 잇는, 장치 종류에 안 묶이는 진동 명령 규격입니다.

**명령 종류(패턴)**: `PREPARE_TURN`, `TURN_NOW`, `UTURN_NOW`, `ARRIVED`, `OFF_ROUTE`, `OBSTACLE_WARNING`, `OBSTACLE_CRITICAL`

**전달 방식**: 서버가 명령을 만들면 순번이 매겨진 채로 버퍼에 쌓이고, 장치(또는 화면의 "진동 명령 로그")가 `GET /api/haptics?after_sequence=N`으로 새 명령만 폴링해서 가져갑니다. 최대 500개까지 보관됩니다.

**담당 코드**: `wayband/haptics.py`(`HapticCommand`, `CommandBuffer`).

---

## 7. ToF 장애물 감지 (시뮬레이터)

벨트에 달릴 4방향 ToF 센서 값을 받아서 경고/위험 상태를 판정하는 기능입니다. 지금은 화면의 "ToF 연동 시험" 패널에서 숫자를 직접 입력해 테스트합니다.

**판정 기준**: 600mm 이하 위험(CRITICAL), 1200mm 이하 경고(WARNING), 1500mm 이상 해제(CLEAR). 0 이하이거나 4000mm 초과는 비정상(INVALID) 처리.

**히스테리시스**: 한 번 경고/위험 상태가 되면 해제 기준(1500mm)을 넘어야 CLEAR로 돌아갑니다. 경계값 근처에서 진동이 떨리듯 반복되는 걸 막기 위함입니다.

**상태 변화가 있을 때만** 새 진동 명령을 만듭니다(같은 상태 반복 시 진동 재발생 안 함).

**담당 코드**: `wayband/hazard.py`(`HazardDetector`), API의 `POST /api/tof`.

---

## 8. 장치 폴링 API (ESP32 게이트웨이용)

실제 하드웨어가 아직 없어도 개발할 수 있도록, 장치가 HTTP로 명령을 받아가는 규격을 먼저 만들어뒀습니다.

- `GET /api/haptics?after_sequence=42&limit=20`: 순번 42 이후의 새 명령만 최대 20개 반환
- 응답에는 `lastSequence`가 포함되어, 장치가 다음 요청 때 어디서부터 받아야 하는지 알 수 있음
- 같은 `commandId`를 중복 실행하지 않도록 장치 쪽에서 기억해두는 걸 권장

**담당 코드**: `docs/HARDWARE_PROTOCOL.md`에 패킷 구조와 안전 설계 원칙(장애물 판정은 서버가 아니라 벨트 MCU가 우선한다 등)이 정리되어 있습니다.

---

## 전체 API 한눈에 보기

| 메서드/경로 | 기능 |
|---|---|
| `GET /api/config` | 카카오 JS 키, 진동 거리 기준 등 공개 설정값 |
| `GET /api/places` | 장소 검색 |
| `POST /api/routes` | 카카오 자동 경로 생성 |
| `GET /api/routes/{route_id}` | 생성된 경로 조회 |
| `POST /api/navigation/{route_id}/location` | GPS 위치 갱신 → 진동 판정 |
| `POST /api/tof` | ToF 4개 값 전달 → 경고/위험 판정 |
| `GET /api/haptics` | 장치가 새 진동 명령 폴링 |
| `POST /api/recordings` | 경로 기록 시작 |
| `POST /api/recordings/{id}/waypoints` | 지점 태깅(좌/우회전, 횡단보도, 계단, 도착) |
| `GET /api/recordings/{id}` | 진행 중인 기록 상태 조회 |
| `POST /api/recordings/{id}/finish` | 기록 종료 → `route_N.json` 저장 |
| `GET /api/recordings` | 저장된 경로 파일 목록 |
| `POST /api/recorded-routes/{route_id}/start` | 저장된 경로로 안내 세션 시작 |
| `POST /api/recorded-routes/{route_id}/location` | GPS 위치 갱신 → 다음 지점 거리·진동 판정 |

---

## 아직 없는 것 (다음 작업 후보)

- 벨트(라즈베리파이) GPS/IMU를 서버가 직접 받는 경로 (지금은 경로 기록·경로 안내 모두 브라우저 GPS 사용)
- 팔찌로 BLE 직접 전송하는 실제 하드웨어 연동 (지금은 HTTP 폴링만 규격화됨)
- 경로/기록 데이터의 영구 저장(DB) — 지금은 서버 재시작 시 진행 중이던 기록·카카오 경로·안내 세션은 사라지고, `route_N.json` 완성 파일만 남음
- 사용자 인증, 여러 명 동시 사용 구분 (기록 모드도, 안내 모드도 동시에 여러 명이 쓰는 상황은 고려 안 됨)
- 저장된 경로 목록 화면에서 삭제/이름 수정, 잘못 태깅한 지점 취소
